# ── Core: runs on BOTH the M3 Pro (CPU backend) and the rented GPU box ──

# Solver
warp-lang>=1.13
numpy>=1.26
scipy>=1.11

# Config & experiment orchestration
hydra-core>=1.3
omegaconf>=2.3
wandb>=0.17

# Geometry (Phase 5: drum mesh, baffles, BVH collision)
trimesh>=4.0

# Analysis & plotting
pandas>=2.0
matplotlib>=3.8
pyvista>=0.44

# Notebooks (Phase 3 validation report is a notebook deliverable)
jupyterlab>=4.0
ipykernel>=6.29

# Testing & quality
pytest>=8.0
pytest-cov>=5.0
ruff>=0.5

# Misc
tqdm>=4.66